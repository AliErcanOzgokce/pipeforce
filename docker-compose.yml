services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: pipeforce
      POSTGRES_PASSWORD: pipeforce
      POSTGRES_DB: pipeforce
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
